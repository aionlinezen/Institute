services:
  - type: web
    name: institute
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python coach_saas_app.py
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 10000